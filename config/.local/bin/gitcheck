#!/usr/bin/env bash
#
# view files with merge conflict markers

if [[ ! "$(command -v batcat)" ]]; then
	less_cmd="less"
else
	less_cmd="batcat"
fi

git diff --staged --check | grep -i conflict | "$less_cmd"
