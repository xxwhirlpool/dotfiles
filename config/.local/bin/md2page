#!/usr/bin/env bash

FILE=$(gum file)
HTML="$HOME/Scripts/pandoc/md2html.html"

if [[ -f "$FILE" ]]; then
	pandoc --standalone "$FILE" --template="$HTML" --no-highlight --wrap=none -o "${FILE%.*}.html"
else
	echo "something went wrong"
	exit 1
fi
