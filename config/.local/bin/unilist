#!/usr/bin/env bash
#
# this is just a chopped up `shmoji`

unidir="$HOME/.config/unilist"
unifile="$unidir/unilist.txt"

uniprint() {
    printf "%s" "$1" | cut -d " " -f 1 | tr -d '\n'
}

unipick=$(cat "$unifile" | dmenu -i -c -l 10) && uniprint "$unipick" | xclip -selection clipboard
