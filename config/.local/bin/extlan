#!/usr/bin/env bash

kr() {
	fnpick=$(gum file)
	unzip -O CP932 "$fnpick"
}

jp() {
	fnpick=$(gum file)
	unzip -O CP932 "$fnpick"
}

choice() {
	choose=$(gum choose "JP" "KR")
	case "$choose" in
		*JP) jp;;
		*KR) kr;;
		*) exit;;
	esac
}

choice
