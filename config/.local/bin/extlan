#!/bin/bash

kr() {
	fnpick=$(gum input --placeholder "file name")
	unzip -O CP932 $fnpick
}

jp() {
	fnpick=$(gum input --placeholder "file name")
	unzip -O CP932 $fnpick
}

choice() {
	choose=$(gum choose "JP" "KR")
	case "$choose" in
		*JP) jp;;
		*KR) kr;;
		*) exit;;
	esac
}

choice
