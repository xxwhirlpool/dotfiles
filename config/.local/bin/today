#!/usr/bin/env bash
#
# current date in ISO format

# if you also want time then supply `time` arg:
# today time
today() {
	if [ $# -eq 0 ]; then
		echo -e "$(date '+%Y-%m-%d')"
	fi

	if [ "$1" == "time" ]; then
		echo -e "$(date '+%Y-%m-%d_%I:%M%p')"
	fi
}

today
