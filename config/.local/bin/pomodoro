#!/bin/bash
#
# adapted from bashbunni's zshrc example:
#
# https://gist.github.com/bashbunni/3880e4194e3f800c4c494de286ebc1d7

notif() {
	notify-send "'$val' session done"
	spd-say -t female3 -r +35 "'$val' session done"
}

quick() {
	val="quick"
	echo -e "⏱️ $val ‧₊˚🌈✩ ₊˚🫧⊹♡" | lolcat -p 0.2
	timer "15m"
	notif
}

work() {
	val="work"
	echo -e "⏱️ $val ‧₊˚🌈✩ ₊˚🫧⊹♡" | lolcat -p 0.2
	timer "45m"
	notif
}

break() {
	val="break"
	echo -e "⏱️ $val ‧₊˚🌈✩ ₊˚🫧⊹♡" | lolcat -p 0.2
	timer "20m"
	notif
}

custom() {
	val="custom"
	choice=$(gum input --placeholder duration | awk '{print $0"m"}' )
	echo -e "⏱️ $val: $choice ‧₊˚🌈✩ ₊˚🫧⊹♡" | lolcat -p 0.2
	timer "$choice"
	notif
}

list() {
	pick=$(gum choose --limit 1 --header "pomodoro timer!" "quick" "work" "break" "custom")
	case "$pick" in
		*quick) quick;;
		*work) work;;
		*break) break;;
		*custom) custom;;
		*) exit;;
	esac
}

list
