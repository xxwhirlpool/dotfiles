--[[
    Author:             Arturo Aguilar Lagunas
    Description:        Dark theme for a single monitor (1920 x 1080)
]]

conky.config = {
    -- window
    alignment = 'top_right',
    gap_x = 0,
    gap_y = 0,
    minimum_height = 1080,
    minimum_width = 460,
    maximum_width = 460,
    background = false,
    border_width = 1,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'normal',
    -- Colors
    font = 'IPAGothic,IPAゴシック:style=regular:size=13',
    font1 = 'IPAGothic,IPAゴシック:style=regular:size=13',
    color1 = '#c6d0f5',                        -- accent
    color2 = '#c6d0f5',                        -- accent dimmed
    own_window_transparent = false,             -- transparency
    own_window_argb_visual = true,            -- transparency
    own_window_argb_value = 50,               -- transparency [0 -255]
    own_window_hints = 'below,undecorated,sticky,skip_taskbar,skip_pager',                  -- if own_window true - just hints - own_window_type sets it
    own_window_colour = '#a1a6be',             -- bg color [comment to full transparency]
    default_color = 'white',                   -- default colors
    default_outline_color = 'white',           -- default colors
    default_shade_color = 'white',             -- default colors
    double_buffer = true,                      -- removes flickering
    draw_borders = false,                      -- borders (window)
    draw_graph_borders = true,                 -- borders (graphs)
    draw_outline = false,                      -- border (text)
    draw_shades = false,                       -- shades
    extra_newline = false,                     -- extra newline at the end when writing to stdout
    -- other
    update_interval = 1.0,
    cpu_avg_samples = 4,
    net_avg_samples = 4,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
    override_utf8_locale = yes
}

conky.text = [[
${voffset 30}

${image /home/kat/.config/conky/wttr.png -p 7,7 -s 460x460}

${voffset 30}
${goto 24}${color1}${font1}System ${voffset 8}${font}  
${goto 24}${color1}OS:        ${color1}$sysname $nodename $machine
${goto 24}${color1}Kernel:    ${color1}$kernel  
${goto 24}${color1}Uptime:    ${color1}$uptime  

${goto 24}${color1}${font1}Memory ${voffset 8}${font}  
${goto 24}${color1}RAM:  ${color1}$mem/$memmax ${alignr}$memperc% ${color1}${membar 4, 124}  
${goto 24}${color1}Swap: ${color1}$swap/$swapmax ${alignr}$swapperc% ${color1}${swapbar 4, 124}${voffset 8}  

${goto 24}${color1}${font1}Now Playing ${voffset 8}${font}
${goto 24}${color1}${execi 5 (python3 /home/kat/Documents/Scripts/lastfm.py)}

]]
